tags = [
  'python',
  'development',
  'tutorials',
  'code',
  'programming',
  'computer science'
]

#tag_range = tags[:-1:2] # the 2 addes two from the starting point
#tag_range = tags[::-1] # this reverses the order of tags


#print(tag_range)

sorted_tags = tags.sort(reverse=True) #return is none, sort returns nothing

print(sorted_tags)